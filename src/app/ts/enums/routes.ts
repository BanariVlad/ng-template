export enum PageRoutes {
  Dashboard = '/dashboard',
  Categories = '/categories',
  Tools = '/tools',
  Help = '/help',
}

export enum ErrorRoutes {
  NotFound = '/error/404',
  Forbidden = '/error/403',
}

export enum ApiRoutes {
  Users = '/users',
  User = '/user',
}
